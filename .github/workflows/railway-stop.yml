name: Stop Railway deployment

on:
  schedule:
    - cron:  '5 5 * * *'

jobs:
  railway-stop:
    if: ${{ secrets.RAILWAY_SCHEDULE == 'true' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install Railway
        run: npm i -g @railway/cli

      - name: Deploy
        run: railway down
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
